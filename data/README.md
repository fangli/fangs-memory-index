# Generated Data

This folder is intentionally mostly empty in the public repository.

It is the mutable, indexable user-data area populated by future runbooks after the operator explicitly asks to initialize or run ingestion.

Default retrieval/indexing should target selected generated subfolders here, not `system/` or `workspace/`.
