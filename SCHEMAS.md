# Schemas Index

Canonical schema docs:

- `schemas/source.md`
- `schemas/atom.md`
- `schemas/view.md`

These are intentionally Markdown-first so generated records can be indexed by a semantic retrieval system. A future deterministic index may mirror these fields into `library/indexes/knowledge.sqlite` or an equivalent local database.
