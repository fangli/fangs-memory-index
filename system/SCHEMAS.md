# Schemas Index

Canonical schema docs:

- `system/schemas/source-record.md`
- `system/schemas/atom-record.md`
- `system/schemas/view-record.md`

These are intentionally Markdown-first so generated records can be indexed by a semantic retrieval system. A future deterministic index may mirror these fields into `data/indexes/knowledge.sqlite` or an equivalent local database.
