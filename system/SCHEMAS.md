# Schemas Index

Canonical schema docs:

- `system/schemas/source-record.md`
- `system/schemas/atom-record.md`
- `system/schemas/view-record.md`
- `system/schemas/index-schema.md`

These are intentionally Markdown-first so generated records can be indexed by a semantic retrieval system. The deterministic index (`data/indexes/knowledge.sqlite`) mirrors atom fields for structured queries during ingestion and view generation.
