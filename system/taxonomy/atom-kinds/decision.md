# decision

A chosen direction, constraint, or explicit decision.

## Required fields

- `kind: decision`
- `decision`
- `decided_at`
- `source_refs`

## Examples

- Use a specific retrieval backend for now.
- Keep the project root out of retrieval paths.
- Store new source requirements in `workspace/source-specs/*.md`.
